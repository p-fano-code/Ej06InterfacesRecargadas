package vehiculos;

public interface Movible {

	/**
	 * Método que se implementará en la clase vehiculo
	 * para indicar los metros que recorrerá cada uno. 
	 * @param distancia
	 */
	public default void avanzar(int distancia) {
		
	}
	
}
